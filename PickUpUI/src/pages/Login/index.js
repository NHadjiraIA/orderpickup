import React from 'react' 
import Login from '../../components/Login'

export const Loginpage = (props) => {
    return (
        <div>
            <Login 
             {...props}
            />
        </div>
    )
}